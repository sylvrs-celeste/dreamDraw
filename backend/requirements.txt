# Pinned to minor versions: this project has a ~1 month lifetime and no CI,
# so reproducible container builds matter more than staying on the newest patch.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

pydantic==2.10.4
pydantic-settings==2.7.0

# Auth. bcrypt is pinned <4.1 because passlib 1.7.4 reads bcrypt.__about__,
# which was removed in 4.1 — the pairing warns noisily otherwise.
PyJWT==2.10.1
passlib==1.7.4
bcrypt==4.0.1

# Image pipeline (step 5) and S3.
Pillow==11.1.0
boto3==1.35.92

python-slugify==8.0.4
